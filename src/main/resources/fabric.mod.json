{
  "schemaVersion": 1,
  "id": "charmony-tweaks",
  "version": "${mod_version}",

  "name": "Charmony Tweaks",
  "description": "Small tweaks that don't change core gameplay.",
  "authors": [
    "svenhjol"
  ],
  "icon": "assets/charmony-tweaks/textures/gui/sprites/icon.png",
  "license": "MIT",
  "environment": "client",
  "mixins": [
    "charmony-tweaks.mixins.json"
  ],
  "accessWidener": "charmony-tweaks.accesswidener",

  "entrypoints": {
    "client": [
      "svenhjol.charmony.tweaks.client.ClientInitializer"
    ]
  },

  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "*",
    "java": ">=21",
    "fabric-api": "*",
    "charmony": "${charmony_version}",
    "charmony-api": "${api_version}"
  },

  "contact": {
    "homepage": "https://charmony.work",
    "sources": "https://github.com/svenhjol/${mod_id}"
  }
}